{
  "name": "onboarding",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "export NODE_OPTIONS=--openssl-legacy-provider; next dev",
    "build": "export NODE_OPTIONS=--openssl-legacy-provider; next build",
    "start": "export NODE_OPTIONS=--openssl-legacy-provider; next start -p 4653",
    "lint": "export NODE_OPTIONS=--openssl-legacy-provider; next lint"
  },
  "dependencies": {
    "aria-modal-dialog": "^3.3.3",
    "axios": "^0.23.0",
    "he": "^1.2.0",
    "html-react-parser": "^1.4.0",
    "intl-tel-input": "^17.0.13",
    "jsonwebtoken": "^8.5.1",
    "next": "latest",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "use-immer": "^0.6.0"
  },
  "devDependencies": {
    "eslint": "7.32.0",
    "eslint-config-next": "11.1.2"
  }
}
